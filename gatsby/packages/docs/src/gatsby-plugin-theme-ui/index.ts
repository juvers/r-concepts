/**
 * Override the theme used by @tishman/components
 * (via gatsby-plugin-theme-ui) with a docz-specific theme.
 *
 * Note that the docz theme _extends_ the @tishman/components
 * theme, so this should be a completely transparent override
 * from the POV of the components.
 */
import {base} from '../gatsby-theme-docz/theme';
export default base;
